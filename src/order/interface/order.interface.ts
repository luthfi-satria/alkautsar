export enum StatusOrder {
  waiting = 'WAITING',
  paid = 'PAID',
  proceed = 'PROCEED',
  success = 'SUCCESS',
  canceled = 'CANCELED',
}

export enum PaymentMethod {
  cash = 'CASH',
  transfer = 'TRANSFER',
  deduction = 'SALARY DEDUCTION',
}
